---
"cnosdb-client": patch
---

Add npm version, CI status, supported Node version, and license badges to the README. The README ships inside the published tarball, so this updates the package page on npm. No runtime code changed.
